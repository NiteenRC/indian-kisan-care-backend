spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_shop_001?createDatabaseIfNotExist=true
    username: root
    password: Root@123
    platform: mysql
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect